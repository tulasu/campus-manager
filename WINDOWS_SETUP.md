# 🪟 Инструкция по запуску Campus Manager на Windows

## 📋 Быстрая настройка

### 1. Создать файл `.env`

Создайте файл `.env` в корне проекта:

```env
GOOGLE_SERVICE_ACCOUNT=service_account.json
GOOGLE_SHEET_ID=ваш_реальный_id_таблицы
DB_CONNECTION_URL=sqlite:///database.db
```

**Как найти GOOGLE_SHEET_ID:**
- Откройте Google таблицу → URL: `https://docs.google.com/spreadsheets/d/{ID}/edit`
- Скопируйте ID между `/d/` и `/edit`

### 2. Настроить доступ к Google таблице

1. Откройте Google таблицу → "Поделиться"
2. Добавьте email: `campus-manager-service@tulsucampusmanager.iam.gserviceaccount.com`
3. Дайте права "Редактор"

### 3. Структура Google таблицы

**Лист 1:** `students_distribution` (ФИО, Пол, Институт, СВО, ЧАЭС, Инвалидность, Курение, Расстояние, Многодетная семья)  
**Лист 2:** `weights` (веса критериев)  
**Лист 3:** `ranking` (создается автоматически)

## 🚀 Запуск приложения

- Убедитесь, что `service_account.json` в корне проекта !!!!!!!!!!!!!!!

```powershell
# Активировать виртуальное окружение
.\venv\Scripts\Activate.ps1

# Запустить сервер
python run.py
```

Сервер запустится на **http://localhost:5500**

**Проверка:**
- http://localhost:5500/docs - Swagger документация
- http://localhost:5500/health - проверка здоровья

## 🔗 Настройка туннеля для Яндекс Формы

Для тестирования интеграции с Яндекс Формой нужен публичный URL:

### Установка localtunnel

```powershell
# Проверьте Node.js
node --version
npm --version

# Если нет Node.js, скачайте с https://nodejs.org/

# Установите localtunnel
npm install -g localtunnel
```

### Запуск туннеля

**Важно:** Запустите в **новом терминале** (сервер должен работать в первом):

```powershell
lt --port 5500
```

Вы получите URL: `https://xxxxx.loca.lt`

### Настройка Яндекс Формы

1. Яндекс Форма → "Интеграции" → "Отправить HTTP-запрос"
2. URL: `https://ВАШ_URL_ИЗ_ТУННЕЛЯ/api/v1/form/submit`
3. Метод: `POST`
4. Заголовки: `Content-Type: application/json`
5. Сохраните

**Тестирование:**
- Отправьте форму → проверьте логи сервера → проверьте Google Sheets (лист 1)

## ⚠️ Решение проблем

**`Google Sheet ID must be a valid non-empty string`**
- Проверьте `.env` файл и `GOOGLE_SHEET_ID`

**`Service account file not found`**
- Убедитесь, что `service_account.json` в корне проекта

**`Google Sheet not found with ID`**
- Проверьте ID таблицы и доступ сервисного аккаунта

**`ModuleNotFoundError: No module named 'core'`**
- Запускайте из корневой директории проекта

**Ошибка активации venv в PowerShell:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

## 📝 Важные замечания

- Порт: **5500** (не 8000)
- Туннель должен работать все время тестирования
- Яндекс Форма отправляет webhook запросы напрямую (заглушка localtunnel не мешает)

## 📚 Дополнительно

- `GOOGLE_SETUP.md` - настройка Google Sheets
- `DISTRIBUTION_README.md` - алгоритм распределения
- `README.md` - общая документация

