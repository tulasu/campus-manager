# 🪟 Инструкция по запуску Campus Manager на Windows

## ✅ Что уже сделано

1. ✅ Создано виртуальное окружение Python (`venv`)
2. ✅ Установлены все зависимости из `requirements.txt`
3. ✅ Файл `service_account.json` присутствует в проекте

## 📋 Что нужно сделать перед запуском

### 1. Создать файл `.env`

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# Google Sheets Configuration
GOOGLE_SERVICE_ACCOUNT=service_account.json
GOOGLE_SHEET_ID=ваш_реальный_id_таблицы

# Database Configuration
DB_CONNECTION_URL=sqlite:///database.db

# Application Settings
APP_NAME=Campus Manager
VERSION=1.0.0
DEBUG=false

# Sheet Indices
STUDENTS_SHEET_INDEX=0
WEIGHTS_SHEET_INDEX=1
RESULTS_SHEET_INDEX=2
```

**ВАЖНО:** Замените `ваш_реальный_id_таблицы` на реальный ID вашей Google таблицы.

#### Как найти GOOGLE_SHEET_ID:
1. Откройте вашу Google таблицу в браузере
2. Посмотрите на URL: `https://docs.google.com/spreadsheets/d/{ЭТО_ГОСУДАРСТВЕННЫЙ_ID}/edit`
3. Скопируйте ID между `/d/` и `/edit`

### 2. Настроить доступ к Google таблице

**КРИТИЧЕСКИ ВАЖНО:** Нужно предоставить доступ сервисному аккаунту к вашей Google таблице:

1. Откройте вашу Google таблицу
2. Нажмите кнопку **"Поделиться"** (Share)
3. Введите email сервисного аккаунта из `service_account.json`:
   ```
   campus-manager-service@tulsucampusmanager.iam.gserviceaccount.com
   ```
4. Дайте права **"Редактор"** (Editor)
5. Нажмите **"Отправить"**

### 3. Проверить структуру Google таблицы

Убедитесь, что в вашей Google таблице есть следующие листы:

**Лист 1 (индекс 0): `students_distribution`**
```
ФИО | Пол | Институт | СВО | ЧАЭС | Инвалидность | Курение | Расстояние | Многодетная семья
```

**Лист 2 (индекс 1): `weights`**
```
Институт | Баллы за институт | СВО | ЧАЭС | Инвалидность | Расстояние | Многодетная семья
```

**Лист 3 (индекс 2): `ranking`** - создается автоматически

## 🚀 Запуск приложения

### Способ 1: Через запускной скрипт (рекомендуется)

```powershell
# Активируйте виртуальное окружение
.\venv\Scripts\Activate.ps1

# Запустите приложение
python run.py
```

Приложение запустится на порту **5500** (не 8000!)

### Способ 2: Через uvicorn напрямую

```powershell
.\venv\Scripts\Activate.ps1
uvicorn main:app --reload --host 0.0.0.0 --port 5500
```

## 🌐 Проверка работы

После запуска приложение будет доступно по следующим адресам:

- **Фронтенд**: http://localhost:5500
- **API документация (Swagger)**: http://localhost:5500/docs
- **Health Check**: http://localhost:5500/health
- **Список студентов**: http://localhost:5500/students
- **Расчет распределения**: POST http://localhost:5500/calculate

## 🧪 Тестирование

### 1. Проверка здоровья приложения

Откройте в браузере или выполните в PowerShell:

```powershell
Invoke-WebRequest -Uri http://localhost:5500/health | Select-Object -ExpandProperty Content
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "message": "Campus Manager is running",
  "version": "1.0.0",
  "debug": false
}
```

### 2. Проверка списка студентов

```powershell
Invoke-WebRequest -Uri http://localhost:5500/students | Select-Object -ExpandProperty Content
```

### 3. Тест расчета распределения

```powershell
Invoke-WebRequest -Uri http://localhost:5500/calculate -Method POST -ContentType "application/json" | Select-Object -ExpandProperty Content
```

Или используйте файл `test_main.http` в вашем редакторе (например, REST Client в VS Code).

### 4. Тест через веб-интерфейс

1. Откройте http://localhost:5500 в браузере
2. Нажмите кнопку **"Распределить"**
3. Дождитесь завершения расчета
4. Проверьте результаты в Google таблице на листе `ranking`

## ⚠️ Возможные ошибки и решения

### Ошибка: `Google Sheet ID must be a valid non-empty string`

**Причина:** Не указан `GOOGLE_SHEET_ID` в `.env` файле или указан неправильно.

**Решение:** 
1. Проверьте наличие файла `.env` в корне проекта
2. Убедитесь, что `GOOGLE_SHEET_ID` указан и содержит реальный ID таблицы

### Ошибка: `Service account file not found`

**Причина:** Файл `service_account.json` не найден.

**Решение:** 
1. Убедитесь, что файл `service_account.json` находится в корне проекта
2. Проверьте путь в `.env`: `GOOGLE_SERVICE_ACCOUNT=service_account.json`

### Ошибка: `Google Sheet not found with ID`

**Причина:** 
1. Неправильный ID таблицы
2. Сервисный аккаунт не имеет доступа к таблице

**Решение:**
1. Проверьте правильность `GOOGLE_SHEET_ID` в `.env`
2. Убедитесь, что вы предоставили доступ сервисному аккаунту к таблице (см. раздел "Настроить доступ к Google таблице")

### Ошибка: `ModuleNotFoundError: No module named 'core'`

**Причина:** Запуск не из корневой директории проекта.

**Решение:**
```powershell
cd C:\Users\ashuverov\Desktop\other-repos\campus-manager
python run.py
```

### Ошибка при активации venv в PowerShell

Если получаете ошибку о политике выполнения скриптов:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

## 📝 Примечания

- Приложение запускается на порту **5500** (согласно `run.py`), а не 8000
- Файл `.env` должен быть в корне проекта
- Убедитесь, что виртуальное окружение активировано перед запуском
- Для работы приложения требуется активное интернет-соединение (для доступа к Google Sheets API)

## 🔍 Дополнительная информация

- Подробная документация по настройке Google Sheets: см. `GOOGLE_SETUP.md`
- Описание алгоритма распределения: см. `DISTRIBUTION_README.md`
- Общая документация проекта: см. `README.md`

