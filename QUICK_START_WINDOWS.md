# 🚀 Быстрый старт на Windows

## ✅ Текущий статус проекта

### Что уже готово:
- ✅ Виртуальное окружение создано (`venv`)
- ✅ Все зависимости установлены
- ✅ Файл `service_account.json` присутствует
- ✅ Структура проекта проверена

### Что нужно сделать:

## 1️⃣ Создать файл `.env`

Создайте файл `.env` в корне проекта:

```env
GOOGLE_SERVICE_ACCOUNT=service_account.json
GOOGLE_SHEET_ID=ВАШ_ID_ТАБЛИЦЫ_ЗДЕСЬ
DB_CONNECTION_URL=sqlite:///database.db
```

**Где взять GOOGLE_SHEET_ID?**
- Откройте вашу Google таблицу
- Посмотрите URL: `https://docs.google.com/spreadsheets/d/{ID}/edit`
- Скопируйте ID между `/d/` и `/edit`

## 2️⃣ Дать доступ сервисному аккаунту

1. Откройте Google таблицу
2. Нажмите "Поделиться"
3. Добавьте email: `campus-manager-service@tulsucampusmanager.iam.gserviceaccount.com`
4. Дайте права "Редактор"

## 3️⃣ Запустить приложение

```powershell
# Активировать виртуальное окружение
.\venv\Scripts\Activate.ps1

# Запустить
python run.py
```

## 4️⃣ Проверить работу

Откройте в браузере:
- http://localhost:5500 - главная страница
- http://localhost:5500/docs - API документация
- http://localhost:5500/health - проверка здоровья

## 📋 Тестирование через PowerShell

```powershell
# Health check
Invoke-WebRequest -Uri http://localhost:5500/health

# Список студентов
Invoke-WebRequest -Uri http://localhost:5500/students

# Расчет распределения
Invoke-WebRequest -Uri http://localhost:5500/calculate -Method POST
```

## ⚠️ Важные замечания

1. **Порт:** Приложение работает на порту **5500**, а не 8000
2. **Файл .env:** Должен быть в корне проекта
3. **Доступ к таблице:** Обязательно предоставьте доступ сервисному аккаунту
4. **Структура таблицы:** Убедитесь, что в Google таблице есть листы:
   - Лист 1: `students_distribution` (студенты)
   - Лист 2: `weights` (веса критериев)
   - Лист 3: `ranking` (создается автоматически)

## 📚 Подробная документация

- `WINDOWS_SETUP.md` - полная инструкция по настройке
- `GOOGLE_SETUP.md` - настройка Google Sheets
- `DISTRIBUTION_README.md` - описание алгоритма
- `README.md` - общая документация

## 🐛 Решение проблем

Если возникли ошибки, см. раздел "Возможные ошибки и решения" в `WINDOWS_SETUP.md`

