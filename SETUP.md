# 🚀 Быстрый запуск Campus Manager

## 1. Установка зависимостей
```bash
# Из корневой директории проекта
pip install -r requirements.txt
```

## 2. Настройка окружения
```bash
# Копируем файл окружения
cp .env.example .env

# Редактируем .env, указывая:
# GOOGLE_SERVICE_ACCOUNT=service_account.json
# GOOGLE_SHEET_ID=ваш_id_таблицы
```

## 3. Запуск приложения (3 способа)

### Способ 1: Через запускной скрипт (рекомендуется)
```bash
python run.py
```

### Способ 2: Прямой запуск (из корневой директории)
```bash
# Убедитесь, что вы в корневой папке campus-manager
python main.py
```

### Способ 3: Через uvicorn
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

## 4. Проверка работы
- 🌐 Фронтенд: http://localhost:8000
- 📚 API docs: http://localhost:8000/docs
- 🎮 Кнопка "Распределить" запускает расчет

## 🔧 Если возникла ошибка импорта

### Ошибка: `ModuleNotFoundError: No module named 'core'`

**Решение 1:** Убедитесь, что вы запускаете из корневой директории
```bash
cd /path/to/campus-manager
python run.py
```

**Решение 2:** Проверьте структуру директорий
```
campus-manager/
├── core/
│   └── __init__.py
├── di/
│   └── __init__.py
├── domain/
│   └── __init__.py
├── handlers/
│   └── __init__.py
├── repositories/
│   └── __init__.py
├── services/
│   └── __init__.py
├── static/
│   └── index.html
├── main.py
├── run.py
└── requirements.txt
```

**Решение 3:** Используйте запускной скрипт
```bash
python run.py  # автоматически добавляет пути
```

## 📋 Минимальные требования для Google Sheets

1. **Лист 1: `students_distribution`**
   ```
   ФИО | Пол | Институт | СВО | ЧАЭС | Инвалидность | Курение | Расстояние | Многодетная семья
   ```

2. **Лист 2: `weights`**
   ```
   Институт | Баллы за институт | СВО | ЧАЭС | Инвалидность | Расстояние | Многодетная семья
   ```

3. **Лист 3: `ranking`** (создается автоматически)

## ✅ Готово к работе!

После запуска система будет доступна по адресу http://localhost:8000 с кнопкой "Распределить".